public class OperatorNot {
    public static void main(String[] args) {
        boolean status = true;

        // Menggunakan operator NOT (!)
        boolean hasil = !status;

        System.out.println("Nilai awal status: " + status);
        System.out.println("Setelah menggunakan operator NOT: " + hasil);
    }
}
